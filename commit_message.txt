fix: add missing } closing brace in checkN_CacheHitCheck

Root cause: the if (!r2.success) block at ~line 1409 was missing its
closing brace }, causing ALL subsequent code (scoreA-E, status, summary,
scoreConfig, the return statement, everything) to be INSIDE the r2-failure
if block. This made all the nested let declarations in sibling if/else
branches conflict (let summary declared 3 times across nested branches
inside the same parent scope), triggering a SyntaxError.

Fix: add the missing } after the r2-failure early return block.

This was the real reason the button didn't work — the entire JS script
failed to parse, window.Doctor was never defined, and Doctor.run() threw.
