UI normalization and stability formula fix

- Fix breakdown UI to use normalized scores from breakdown object
- Use breakdown.coreCompatibility.score instead of checks.basicCompatibility.score
- Use breakdown.clientConfig.score instead of checks.clientConfig.score
- Use breakdown risk for moduleSection color, not check status
- Fix stability avgLatencyScore threshold: 3500ms = 4/8
- Fix stability jitterScore: ratio 3.0-4.0 = 2/5, ratio 4.0-5.0 = 1/5
- Add Case 16: screenshot_stability_and_normalization test
- Cache bust: v=20260522-rc17f
