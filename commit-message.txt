fix: restore single-tool-page layout - CSS classes now match HTML structure

- Rewrite index.html and en/index.html to use single-tool-page layout
  with .tool-hero, .center-tool-shell, .doctor-card, .config-paste classes
- Add missing CSS: .single-tool-page, .tool-hero, .center-tool-shell,
  .doctor-card, .config-paste, .floating-plugin, .btn-secondary,
  .field-help, .cost-hint, .result-empty-state, .site-logo__mark
- Add oninput/onclick bindings to textarea and parse-config button
- Header now uses .site-logo__mark (text-based, no SVG dependency)
- Footer simplified: just logo name, nav links, disclaimer, copyright
- Remove plugin-corner div (now floating-plugin anchor)
- API key never appears in report, image, clipboard, localStorage, or URL
