feat: upgrade to black-box API scorecard product

Homepage: rename to API Doctor / AI API 黑盒验货, update hero tagline,
button text, and cost hint copy.

Quick check: completely rewrite report as scorecard with:
- A-F grade system (A=硬货, B=能用, C=掺水, D=疑似空跑, F=高危, U=验不出真身)
- Numeric score with capping rules (HTTP 200 + no output + no usage = D/55 max)
- Color-coded scorecard hero (56px score display)
- Risk chip tags (疑似空跑, usage 消失, 接口假健康, etc.)
- Evidence grid (HTTP, Latency, Visible Output, Usage, tokens, Model, Base URL)

Copy & image: rewrite copyOneLine, copyForum, copyForProvider, buildQuickImageNode
to share-score format: "我的 API Doctor 验货：D档 55分｜疑似空跑｜报告 ID：xxx"

Full report: sync terminology to pass/risky/suspect/unverified,
update report title to API Doctor 完整验货分,
revise disclaimer language.

Also: fix undeclared `lang` variable in runDiagnosis().
